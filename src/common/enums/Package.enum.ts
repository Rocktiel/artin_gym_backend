// Paket Süre Tipi
export enum PackageDurationType {
  DAY = 'DAY',
  WEEK = 'WEEK',
  MONTH = 'MONTH',
  YEAR = 'YEAR',
}

// Paket Durumu
export enum PackageStatus {
  ACTIVE = 'ACTIVE', // Satışta
  INACTIVE = 'INACTIVE', // Satıştan kalkmış
}
